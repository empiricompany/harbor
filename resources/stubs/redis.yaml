services:
  redis:
    image: redis:7.4
    profiles: [redis]
    command: redis-server --maxmemory-policy volatile-lfu
    restart: unless-stopped
    tmpfs:
      - /data
    ports:
      - "6379:6379"
    networks:
      - harbor
