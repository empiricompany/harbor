services:
  adminer:
    image: adminer:4
    profiles: [adminer]
    environment:
      ADMINER_DEFAULT_SERVER: db
    ports:
      - "${HARBOR_ADMINER_PORT:-8082}:8080"
    networks:
      - harbor
